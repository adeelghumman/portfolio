import React from 'react'

const HPcomponent = (props) => {
    return (
        <div>

            <h1>{props.headingData} </h1>
            <p id="bs4c">{props.paragraphData}</p>

        </div>
    )
}

export default HPcomponent
